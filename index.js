let frase = "Look around look around, how lucky we are to be alive right now".toLocaleLowerCase();
frase = frase.split("");


console.log(frase);
console.log(contadorRepetidas);



function FirstLetterUnrepeated(frase) {
  let contadorRepetidas = [];
  let contadorNoRepetidas = [];
  let primera = frase.indexOf(FirstLetterUnrepeated);

  for (let i = 0; i < frase.length; i++) {
    const letra = frase[i];

    if (frase.indexOf(letra, i + 1)) {
        contadorRepetidas.push();
        
    }
  }
}